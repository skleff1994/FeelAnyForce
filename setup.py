from setuptools import setup, find_packages

setup(
    name="feelanyforce",
    description="FeelAnyForce",
    packages=find_packages(),
    install_requires=[
    ],
    python_requires=">=3.9",
    entry_points={
    },
    classifiers=[
        "Programming Language :: Python :: 3",
    ],
)
